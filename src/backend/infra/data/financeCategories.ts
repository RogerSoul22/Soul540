// Fonte única movida para `shared/financeCategories.ts` (assim franchise e factory,
// que só têm o alias @shared, também conseguem importar as mesmas categorias DRE).
// Este arquivo existe apenas para manter compatível o import já usado no app main.
export * from '@shared/financeCategories';
